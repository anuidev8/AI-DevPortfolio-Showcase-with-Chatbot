[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "noshy-voice-host"
version = "1.0.0"
description = "NoShy Voice — LiveKit networking host that interviews guests and drives the match UI"
requires-python = ">=3.12, <3.15"

dependencies = [
    "livekit-agents[images]>=1.7.0",
    "livekit-plugins-ai-coustics>=0.3.2",
    "livekit-plugins-elevenlabs>=1.7.0",
    "python-dotenv",
]

[dependency-groups]
dev = [
    "ruff",
]

[tool.uv]
constraint-dependencies = ["yarl<1.24"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.setuptools.package-dir]
"" = "src"

[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "W", "I"]
ignore = ["E501"]
